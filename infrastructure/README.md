# Infrastructure

The application is intentionally local-first at this stage. There is no Docker, Terraform, CloudFormation, or cloud deployment configuration yet.

When production polish is planned, document the chosen deployment target and add only the infrastructure needed for that target. Until then, this folder prevents deployment concerns from being mixed into the application code.
